# Configuración de ejemplo para poste.io personalizado
# Copia este archivo a .env y ajusta los valores según tu entorno

# Hostname de tu servidor de correo (OBLIGATORIO)
HOSTNAME=mail.ejemplo.com

# Puertos personalizados
HTTP_PORT=8080
HTTPS_PORT=8443

# Configuración de IPs
# host = usa las IPs del hostname
# * = escucha en todas las IPs disponibles
# 1.2.3.4 5.6.7.8 = lista específica de IPs
LISTEN_ON=host
SEND_ON=

# Zona horaria
TZ=America/Mexico_City

# Desactivar servicios opcionales (para reducir uso de recursos)
DISABLE_CLAMAV=TRUE
DISABLE_RSPAMD=FALSE

# Clave DES persistente para plugins de Roundcube (48 caracteres hex)
# Genera una con: openssl rand -hex 24
# DES_KEY=